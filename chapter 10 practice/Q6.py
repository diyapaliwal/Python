# Can you change the self-parameter inside a class to something else (say “diya”). Try changing self to “slf” or “diya” and see the effects.

class testing:
    def greet(slf):
        print("how are u bud")
        
d = testing()
d.greet()